name: "dbt_warehouse"
version: "1.0.0"
config-version: 2

profile: "dbt_warehouse"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_warehouse:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

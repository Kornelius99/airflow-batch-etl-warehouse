# airflow-batch-etl-warehouse
Dockerized end-to-end batch ELT pipeline: Airflow orhestrates extraction from Postgres, staging, and dbttransformations into a mini analytics warehouse. Cloe and run locally with docker-compose.
